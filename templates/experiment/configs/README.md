# Configs

Store only lightweight files needed to understand or reproduce the experiment:

- YAML/JSON/TOML configs.
- Small copied summary CSV files.
- Source design or plan notes when they directly explain the package.

Do not store checkpoints, rollouts, videos, logs, or copied `outputs/` trees.

# G1 Body Tracking WBC

This experiment family tracks Unitree G1 body-only motion tracking under WBC or
WBC-like rollout optimization. It currently covers `spider` results and reserves
space for future `dial_mpc` results.

This family excludes dexhand-specific manipulation and full-body dexhand
experiments. Those should receive separate family directories when they start.


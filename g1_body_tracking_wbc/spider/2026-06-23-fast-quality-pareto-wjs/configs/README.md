# Configs

- `source_commands.csv`: filtered command provenance from the sync package.
- `source_milestone_snapshot.md`: historical milestone note used as context.
- `summaries/*.csv`: copied lightweight summary CSVs for this package.

No raw rollouts, videos, logs, checkpoints, or copied `raw/` metrics are stored here.

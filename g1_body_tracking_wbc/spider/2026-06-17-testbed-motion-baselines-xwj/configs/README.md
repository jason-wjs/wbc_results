# Configs

- `g1_wbc_reward_weights_method_specific_v14_20260612.json`: copied reward
  weights used by the packaged Spider-native WBC baseline runs.

The no-MPC rows in `primary_metrics.csv` are reference anchors. The primary
Spider-native baseline comparison is the 9-row `g1_wbc_*` subset.

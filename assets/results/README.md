# Versioned Result Assets

This directory is reserved for explicitly approved result artifacts that need to
be versioned with the registry.

The default policy remains: raw experiment artifacts stay outside git and are
indexed from experiment packages. Any file added here must also be allowed by
`scripts/validate_registry.py`.
